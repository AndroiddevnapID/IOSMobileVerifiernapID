import XCTest
@testable import TestMobileVerifier

final class TestMobileVerifierTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TestMobileVerifier().text, "Hello, World!")
    }
}
